# Ant build properties

verbose.mode=off

app.name=WorldEngine
app.version=0.04
app.author=Ethan Blackwelder
app.website=https://github.com/eib/java-graphics-engine

src.home=${basedir}${file.separator}src
demos.home=${basedir}${file.separator}demos

build.home=${basedir}${file.separator}build
build.lite=${build.home}${file.separator}lite
build.full=${build.home}${file.separator}full
dist.home=${basedir}${file.separator}dist

jar-full.name=${app.name}-${app.version}-full.jar
jar-lite.name=${app.name}-${app.version}-lite.jar
jar.target=1.6
jar.main-class=com.eblackwelder.graphics.GraphicsMain

run.main-class=${jar.main-class}
